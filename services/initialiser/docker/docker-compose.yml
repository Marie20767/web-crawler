services:
  url-initialiser:
    build:
      context: ../../.. 
      dockerfile: services/initialiser/docker/Dockerfile
    container_name: url-initialiser
    depends_on:
      kafka-init:
        condition: service_completed_successfully
    env_file:
      .env.docker
    restart: on-failure
